# Basic test config for a vm, cmdline module added so uuid doesn't need to be valid


modules = [
  "ugrd.base.base",
  "ugrd.base.cmdline",
  "ugrd.kmod.vm",
]

validate = false

#kernel_version = "6.1.57-gentoo-custom"

[mounts.root.source]
uuid  = '06eb29ad-20a1-410e-965a-3f89aea5e07a'
